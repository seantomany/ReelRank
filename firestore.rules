rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through the API backend (Firebase Admin SDK),
    // which bypasses these rules. Block direct client access.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
